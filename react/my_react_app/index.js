import React from 'react';
import ReactDOM from 'react-dom/client';
import Api from "./jsx/app1";


ReactDOM.createRoot(document.getElementById('react-app')).render(
    <React.StrictMode>
            <Api />
    </React.StrictMode>,

)